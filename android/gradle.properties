org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
android.suppressUnsupportedOptionWarnings=android.enableJetifier

# ??? 16 KB page size support ?????????????????????????????????????????????????
# AGP 8.5+ strict verification: ensures all load segments in .so files are
# aligned to 16 KB boundaries (needed for Android 15+ / Google Play requirement).
android.experimental.usePalignedPackaging=true

